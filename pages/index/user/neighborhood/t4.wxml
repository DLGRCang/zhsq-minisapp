<have-content id="haveTrue" bind:addInfo="getAddInfo"></have-content>
<view class="tabllq" style="top:{{CustomBar}}px">
      <scroll-view scroll-x class="nav" style="color:#eee" scroll-with-animation scroll-left="{{scrollLeft}}">
        <view class="cu-item {{index==TabCur?'text-bai cur':''}}" wx:for="{{bqList}}" wx:key="index" bindtap="tabSelect" data-i="{{index}}" data-id="{{item.dictionariesId}}">
          {{item.dictionariesName}}
        </view>
      </scroll-view>
    </view>
<view class="t4" style="{{rows.length != 0 ? 'padding-top:100rpx':''}}">
  
<view class="head_content">

<view class="cu-card dynamic {{isCard?'no-card':''}}">
<!-- 第一部分 内容 -->
<view class="queshiye" wx:if="{{rows.length == 0}}" style="padding-top:600rpx">
    <image src="https://yiqi.sucstep.com/zhsq/assets/images/applets/queshi.png" class="imgs"></image>
    <view class="wenzi">暂无数据</view>
  </view>
  <view class="cu-item shadow" wx:for="{{rows}}" wx:key="index" bindtap="lljClick" data-rows="{{item}}">
    <view class="cu-list menu-avatar">
      <view class="cu-item">
        <view class="cu-avatar round lg" style="background-image:url(https://ossweb-img.qq.com/images/lol/web201310/skin/big10006.jpg);"></view>
        <view class="content flex-sub">
          <view>小区用户名</view>
          <view class="text-gray text-sm flex justify-between">
            {{item.gmtCreate}}
          </view>
        </view>
      </view>
    </view>
    <view class="text-cont">
      <view wx:if="{{item.pantrue == true}}">{{item.message1}}<text style="color:green;margin-left:10rpx" catchtap="quanwen" data-i="{{index}}">全文</text></view>
      <view wx:if="{{item.pantrue == false}}">{{item.message}}<text style="color:green;margin-left:10rpx" wx:if="{{item.pantrue1 == true}}" catchtap="squanwen" data-i="{{index}}">收起</text></view>
      
    </view>
    <!-- <view wx:for="{{item.img}}" wx:for-index="i" wx:for-item="items">{{items}}</view> -->
   <!-- <view class="bg-white padding" wx:if="{{item.file != ''}}">
    <view class="imgFor" style="{{item.img.length >= 3?'justify-content: space-between':''}}">
        <image wx:for="{{item.img}}" wx:for-index="i" wx:for-item="items" wx:if="{{i<=2}}" style="{{item.img.length == 2?'margin-right:15rpx':''}};width:200rpx;height:200rpx" src="http://172.16.20.81:9000/fileService/downloadFTP/public/{{items}}"></image>
        
    </view>
  </view> -->
  <view class="bg-white padding" style="background:#fff" wx:if="{{item.file != ''}}">
    <view class="grid col-3 grid-square">
      <view class="bg-img" wx:for="{{item.img}}" wx:for-index="i" wx:for-item="items" wx:if="{{i<=2}}" wx:key="index" style="background-image:url(http://172.16.20.81:9000/fileService/downloadFTP/public/{{items}});"></view>
    </view>
  </view>
    <!-- <view class="text-gray text-sm text-left padding padding-right">
   
      <view  class="download" bindtap="toStar" style="margin-left:0;">
      <block wx:if="{{isStar == false}}">
        <image src="https://yiqi.sucstep.com/zhsq/assets/images/applets/star_active.png " class="star_img">  </image><text class="text-gray"> 已收藏</text> 
      </block>
      <block wx:elif="{{isStar == true}}" >
        <image src="https://yiqi.sucstep.com/zhsq/assets/images/applets/star.png " class="star_img">  </image><text class="text-gray"> 收藏</text> 
      </block>
      </view>
      
    
    <view class="download" bindtap="toShare">
      <block wx:if="{{isShare == false}}">
        <image src="https://yiqi.sucstep.com/zhsq/assets/images/applets/share.png " class="share_img">  </image><text class="text-gray"> 分享</text> 
      </block>
      <block wx:elif="{{isShare == true}}" >
        <image src="https://yiqi.sucstep.com/zhsq/assets/images/applets/share.png " class="share_img">  </image><text class="text-gray"> 已分享</text> 
      </block>
      </view>
      <image src="https://yiqi.sucstep.com/zhsq/assets/images/applets/message.png " class="message_img" style="margin-left:25rpx;"></image><text class="text-gray"> 30</text> 
            
      <image src="https://yiqi.sucstep.com/zhsq/assets/images/applets/good_active.png " class="good_img"></image><text class="right" style="padding-right:0;"> 15</text> 
    </view> -->
    <view class="pinglun">
        <view></view>
  
      <view class="pinglun-right" catchtap>
        <view catchtap="dzClick" data-i="{{index}}">
          <text class="cuIcon-like" wx:if="{{!item.dianzan}}"></text>
          <text class="cuIcon-likefill" style="color:red" wx:if="{{item.dianzan}}"></text>
          <text>{{item.dianzanshu > 99 ? '99+':''+item.dianzanshu}}</text>
        </view>
        
        <view catchtap="plClick" data-i="{{index}}">
          <text class="cuIcon-comment"></text>
          <text>30</text>
        </view>
        
        <view catchtap="scClick" data-i="{{index}}">
          <text class="cuIcon-favor" wx:if="{{!item.shoucang}}"></text>
          <text class="cuIcon-favorfill" style="color:yellow" wx:if="{{item.shoucang}}"></text>
          <text>{{item.shoucang ? '已收藏':'收藏'}}</text>
        </view>
      </view>
      

      
    </view>
    <view class="pinglunnr" catchtap wx:if="{{item.pinglun}}">
      <view wx:for="{{3}}" wx:for-index="indexD" class="pinglunlb" bindtap="gbzezhao">
        <view class="nr-top">
          <view class="nr-name">
            <text>德力格日仓</text>

          </view>
          <view class="nr-box" catchtap="plzplClick" data-index="{{index}}" data-indexd="{{indexD}}">
              评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容评论内容
          </view>
        </view>
        <view class="nr-bot">
          <view>2020年10月12日</view>
          <view class="bot-last">
            <view catchtap="plzplClick" data-index="{{index}}" data-indexd="{{indexD}}">
              <text class="cuIcon-comment"></text>
              <text>评论</text>
            </view>
            
            <text class="cuIcon-likefill" style="color:red"></text>
          </view>
          <view class="plzpl" catchtap wx:if="{{index == plInputTrue && indexD == plInputTrueL}}">
            <input type="text" class="inputkuang" focus="{{index == plInputTrue && indexD == plInputTrueL ? 'auto':''}}" bindinput="inputClick1"/>
            <view class="plzplnr" catchtap="plzplClick1" data-index="{{index}}" data-indexd="{{indexD}}">
              {{plzplnrContent}}
            </view>
            <view class="plzplan">评论</view>
          </view>
        </view>
      </view>
      <input type="text" class="inputkuang" focus="{{index == inputtrue ? 'auto':''}}" bindinput="inputClick"/>
        <view class="plkuang" catchtap>
          <view class="fdpl" bindtap="tianxie" data-index="{{index}}">
            {{contentpl}}
          </view>
          <view class="zplan">评论</view>
        </view>
    </view>
 </view>
</view>
</view>

<!-- 发布图标 -->
<button class="cuIcon-add bg-blue fabu_btnLlq" role="button" aria-disabled="false" bindtap="fabu"></button>

</view>
